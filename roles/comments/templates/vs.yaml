apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: "{{app_name}}"
  namespace: "{{namespace}}"
spec:
  hosts:
    - "{{app_name}}"
  http:
  - route:
    - destination:
        host: comments.nemo.svc.cluster.local
        subset: v1
