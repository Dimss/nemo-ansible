apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: "{{app_name}}"
  namespace: "{{namespace}}"
spec:
  host: "{{app_name}}"
  trafficPolicy:
    tls:
      mode: "{{mtls_mode}}"
  subsets:
  - name: v1
    labels:
      version: v1
