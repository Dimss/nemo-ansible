apiVersion: v1
kind: Service
metadata:
  name: receiver
  namespace: "{{namespace}}"
  lables:
    app: receiver
spec:
  selector:
    app: receiver
  ports:
  - name: http
    port: 80
    targetPort: 8080
---
kind: DeploymentConfig
apiVersion: "v1"
metadata:
  name: "{{app_name}}"
  namespace: "{{namespace}}"
  labels:
    app: "{{app_name}}"
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: "{{app_name}}"
    spec:
      containers:
      - name: "{{app_name}}"
        image: " "
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: prod
        ports:
        - containerPort: 8080
  triggers:
    - type: "ConfigChange"
    - type: "ImageChange"
      imageChangeParams:
        automatic: true
        containerNames:
          - "{{app_name}}"
        from:
          kind: "ImageStreamTag"
          name: "{{app_istag}}"