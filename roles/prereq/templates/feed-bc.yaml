kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "feed"
  namespace: "{{namespace}}"
spec:
  runPolicy: "Serial"
  source:
    git:
      uri: "https://github.com/Dimss/nemo-feed.git"
  strategy:
    sourceStrategy:
      from:
        kind: "ImageStreamTag"
        name: "java:8"
        namespace: "openshift"
  output:
    to:
      kind: "ImageStreamTag"
      name: "feed:0.1"
